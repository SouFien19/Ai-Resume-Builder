import { TemplateDef } from "./template-manager";
import { ResumeData } from "@/types/resume";

export function renderTemplate(_template: TemplateDef, _data: ResumeData) {
  // Placeholder for server-side PDF or client-side preview render pipeline.
  // For now, LivePreview.tsx handles visual rendering.
  return null;
}
