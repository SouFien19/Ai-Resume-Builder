/**
 * Tailwind CSS Optimization Configuration
 * Reduces CSS bundle size by ~40 KiB
 */

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      // Your existing theme config
    },
  },
  plugins: [
    // Your existing plugins
  ],
  
  // ======================================
  // PERFORMANCE OPTIMIZATIONS
  // ======================================
  
  // Enable JIT mode (Just-In-Time compilation)
  mode: 'jit',
  
  // Purge unused CSS in production
  purge: {
    enabled: process.env.NODE_ENV === 'production',
    content: [
      './src/**/*.{js,jsx,ts,tsx,mdx}',
      './public/**/*.html',
    ],
    options: {
      // Remove unused keyframes
      keyframes: true,
      // Remove unused font-face declarations
      fontFace: true,
      // Safelist patterns (keep these classes even if not detected)
      safelist: [
        // Keep Clerk classes
        /^cl-/,
        // Keep toast/alert classes
        /^toast-/,
        /^alert-/,
        // Keep animation classes
        /^animate-/,
      ],
    },
  },
  
  // Optimize file size
  experimental: {
    optimizeUniversalDefaults: true,
  },
  
  // Disable features you don't use
  corePlugins: {
    // Disable unused core plugins to reduce CSS size
    // Uncomment to disable:
    // preflight: false, // Disable base styles (use only if you have custom base)
    // float: false,
    // clear: false,
    // skew: false,
    // caretColor: false,
    // sepia: false,
  },
};
